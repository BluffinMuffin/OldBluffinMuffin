package backend.agent;

/**
 * @author Hocus
 *         This enum represents type of simplified poker action.
 */
public enum TypeSimplifiedAction
{
    FOLD, CHECK, CALL, RAISE_CALL, RAISE, CALL_RAISE, RAISE_RAISE
}
