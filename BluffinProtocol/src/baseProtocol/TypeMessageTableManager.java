package baseProtocol;

/**
 * @author Hocus
 *         This enum list the type of message that the table manager can receive
 */
public enum TypeMessageTableManager
{
    AUTHENTIFICATION, JOIN_TABLE, DISCONNECT
}
